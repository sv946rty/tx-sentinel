# Database Configuration
# Supabase PostgreSQL connection string (Transaction mode / port 6543)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-ID].supabase.co:6543/postgres

# Database schema name (never use 'public')
DATABASE_SCHEMA=ai_agent

# Authentication Configuration
# Secret key for Better Auth (min 32 characters)
AUTH_SECRET=your-secret-key-at-least-32-characters-long

# Google OAuth (https://console.developers.google.com/)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth (https://github.com/settings/developers)
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-api-key

# Session Password Protection (Optional - for demo deployment)
# Set to "true" to require a password before users can ask questions
# This helps prevent abuse and excessive API costs on public demos
REQUIRE_SESSION_PASSWORD=false
SESSION_PASSWORD=whoami
